<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SecureStash" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/brandeisblue</item>
        <item name="colorSecondary">@color/unblue</item>
        <item name="colorAccent">@color/isabelline</item>
        <item name="colorError">@color/scarlet</item>
    </style>

    <style name="Theme.SecureStash" parent="Base.Theme.SecureStash" />
</resources>